% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formr_api.R
\name{formr_get_results}
\alias{formr_get_results}
\title{Get results from the formr API (User-Friendly Wrapper)}
\usage{
formr_get_results(run_name, surveys = NULL, sessions = NULL)
}
\arguments{
\item{run_name}{The name of the run (required).}

\item{surveys}{A named list, where names are survey names and values are
character vectors of items. e.g., \code{list(survey_a = c("item1", "item2"))}.
Use \code{NULL} as the value to get all items for that survey.}

\item{sessions}{A character vector of session IDs to filter for.}
}
\description{
After authenticating with \code{\link[=formr_api_access_token]{formr_api_access_token()}}, this function
fetches results with easy-to-use R arguments.
}
\examples{
\dontrun{
# First, authenticate
formr_api_access_token(client_id = 'your_id', client_secret = 'your_secret')

# --- Example 1: Get all data from the 'widgets' run ---
all_data <- formr_get_results(run_name = "widgets")

# --- Example 2: Get specific items from two surveys ---
my_surveys <- list(
  all_widgets = c("abode", "mc_god"),
  demographics = c("age", "gender")
)
survey_data <- formr_get_results(run_name = "widgets", surveys = my_surveys)

# --- Example 3: Get data for specific users ---
my_sessions <- c("session_id_abc", "session_id_xyz")
user_data <- formr_get_results(run_name = "widgets", sessions = my_sessions)

}
}
